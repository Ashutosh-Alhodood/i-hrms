version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    # point Amplify to the static export directory created by Next output: "export"
    baseDirectory: out
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*