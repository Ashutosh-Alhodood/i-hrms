version: 1
frontend:
  phases:
    preBuild:
      commands:
        - node -v
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    # Amplify will publish the static files inside "out"
    baseDirectory: out
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*